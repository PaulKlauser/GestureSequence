dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "GestureDetector"
include ':sample'
include ':gesturesequence'
include ':gesturesequencetest'
